import { SafeAreaView, FlatList, Text, View } from 'react-native';

export default PodcastList = () => {
    return (
        <SafeAreaView>
            <FlatList>

            </FlatList>
        </SafeAreaView>
    )
}